[project]
name = "mission-todolist"
version = "0.1.0"
description = "Local-first Windows desktop TodoList/task-growth app prototype"
requires-python = ">=3.12"

[project.scripts]
mission-todolist = "mission_app.app:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
